# EatBotFront - Bot Controller Interface

Минималистичный интерфейс для управления ботом в реальном времени с использованием Vite, React и WebSocket.

## Особенности

- 🎨 Современный минималистичный дизайн в тёмной теме
- 🔌 WebSocket подключение для реал-тайм обновлений состояния
- ⚡ Быстрая загрузка с Vite
- 📱 Адаптивный дизайн (мобильные устройства поддерживаются)
- 🎯 TypeScript для надёжности типов
- 🔧 CSS Modules для изоляции стилей

## Требования

- Node.js 16+
- npm или yarn

## Установка

```bash
cd eatbotfront
npm install
```

## Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

## Запуск в production

```bash
npm run build
npm run preview
```

## Структура проекта

```
src/
├── components/
│   └── BotController/
│       ├── BotController.tsx          # Основной компонент управления ботом
│       └── BotController.module.css   # CSS модуль с стилями
├── types/
│   └── api.ts                        # Типы для API
├── App.tsx                           # Главный компонент приложения
├── App.css                           # Глобальные стили
├── main.tsx                          # Точка входа
└── index.css                         # Базовые стили и переменные CSS
```

## API Эндпоинты

Приложение работает со следующими эндпоинтами сервера:

- `GET /api/bot/state` - Получить текущее состояние бота
- `POST /api/bot/enable` - Включить бота
- `POST /api/bot/disable` - Отключить бота
- `POST /api/bot/toggle` - Переключить состояние бота

## WebSocket

Приложение подключается к WebSocket на `ws://localhost:3000` для получения обновлений состояния бота в реальном времени.

Ожидаемый формат сообщения:

```json
{
  "type": "state",
  "isEnabled": true
}
```

## Функциональность

### BotController компонент

Основной компонент отображает:

1. **Status Badge** - показывает текущее состояние (Active/Inactive)
2. **Главная кнопка** - для включения/отключения бота
3. **Информация об ошибках** - отображает ошибки при возникновении

Компонент автоматически:

- Загружает состояние при монтировании
- Подключается к WebSocket для получения обновлений
- Отправляет запросы на изменение состояния через REST API
- Обновляет UI на основе ответа сервера

## Стилизация

Проект использует **CSS Modules** для полной изоляции стилей:

```typescript
import s from "./BotController.module.css";

// Использование
<button className={s.button} />
<div className={s.container} />
```

### Цветовая схема

- **Фон**: Градиент от #1a1a2e к #16213e
- **Активное состояние**: Зелёный (#22c55e)
- **Неактивное состояние**: Красный (#ef4444)
- **Текст**: Белый с прозрачностью для иерархии

## Скрипты

- `npm run dev` - Запустить dev сервер
- `npm run build` - Собрать для production
- `npm run preview` - Просмотр собранного проекта

## Browser Support

Поддерживаются все современные браузеры:

- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

## Разработка

### Добавление новых функций

1. Создайте новый компонент в `src/components/`
2. Создайте соответствующий CSS Module файл
3. Импортируйте компонент в нужном месте

### Пример структуры компонента

```typescript
import React from 'react';
import s from './MyComponent.module.css';

interface Props {
  // Ваши props
}

export const MyComponent: React.FC<Props> = (props) => {
  return (
    <div className={s.container}>
      {/* Контент */}
    </div>
  );
};
```

## Лицензия

MIT

```

```
